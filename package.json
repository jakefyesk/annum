{
  "name": "annum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A year, one dot at a time.",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/optimise.mjs && node scripts/site.mjs",
    "guard": "node scripts/guard.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "sharp": "^0.35.4"
  }
}
