name: build & deploy

# No schedule. Every day of the year is rendered ahead of time and the phone
# picks today's file, so there is nothing for a cron job to keep up to date.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Second line of defence: the pre-commit hook can be bypassed, this cannot.
      - name: Guard against personal data in the repo
        run: npm run guard

      - name: Build
        env:
          # Read from the SECRET, not the variable. The runner prints a step's
          # env: block verbatim, and only secrets are masked there — a variable
          # would land in this repo's public logs in clear base64.
          #
          # scripts/events.mjs writes the secret and the variable together: the
          # secret is what CI consumes, the variable is the readable mirror that
          # makes the config editable in conversation, since secrets are
          # write-only. Always edit through that script so the two can't drift.
          ANNUM_CONFIG: ${{ secrets.ANNUM_CONFIG }}
          ANNUM_SLUG: ${{ secrets.ANNUM_SLUG }}
        run: npm run build

      - name: Check nothing personal leaked into the payload
        run: node scripts/verify-dist.mjs

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
